[package]
name = "baremetal-asan-rt"
version = "0.1.0"
edition = "2024"

[features]
mve = []

[dependencies]
embassy-sync = "=0.8.0"

[build-dependencies]
cc = "1.2"

[dev-dependencies]
critical-section = { version = "1.2", features = ["std"] }
